x = int(input("Enter a number of repetitions"))

# Decorator function to repeat the function call
def repeat_decorator(func):
    def wrapper():
        for _ in range(x):
            func()
    return wrapper

@repeat_decorator
def hello():
    print('Hello')

hello()
